spring:
  cloud:
    gateway:
      routes:
        - id: shortlink-user-service
          uri: lb://shortlink-user-service
          predicates:
            - Path=/api/short-link/admin/**
          filters:
            - name: TokenValidate
              args:
                whitePathList:
                  - /api/short-link/admin/v1/user/login
                  - /api/short-link/admin/v1/user/has-username

        - id: short-link-project
          uri: lb://short-link-project
          predicates:
            - Path=/api/short-link/**
          filters:
            - name: TokenValidate
              args:
                whitePathList: []
