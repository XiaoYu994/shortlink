# 异步高德后：1 万短码跳转复测

- 时间：2026-08-21 11:09:21–11:10:22 CST
- 镜像：aggregation 已换成 `12c63cf`+`41b6995`（高德异步）
- 负载：与场 B 相同，`mix.csv` 20 线程 / 10s 爬坡 / 60s

对照场 B（高德仍在消费者里同步）：213,664 次，3,579/s，P99 19ms，访问日志跟上约 1.3%。

## 跳转

| 指标 | 本轮 | 场 B |
|---|---|---|
| 样本 | 219,878 | 213,664 |
| 错误 | **0** | 0 |
| 吞吐 | **3,686/s** | 3,579/s |
| 平均 / P99 | 4.8ms / **11ms** | 5.0ms / 19ms |
| 缓存 | 219,878 hit / 0 miss | 99.78% |

跳转仍然合格，P99 略好。缓存全命中（Redis 里还留着上一轮的 goto）。

## 统计入库（这轮要看的）

本机访客 IP 全是 `172.29.0.1`，新代码当内网，**完全不调高德**（`IP解析失败` 0 次，`locale_enrich_dropped` 0）。

| 时刻 | 访问日志增量 | MQ consume success |
|---|---|---|
| 压测 60s 内 | +2,971 | +2,968 |
| 再等 20s | 累计 +3,950 | — |
| 再等约 30s | 累计约 +6,300 | 7,834（含启动后积压） |

高峰 3.6k 跳转 vs 消费大约 **50 条/秒**，跟上率仍约 **1.4%**。压测结束后消费能到约 100 条/秒，积压还在消化，远没填完。

结论：高德已经不是这条本机流量的瓶颈。剩下是 **每条消息多条 SQL + 锁** 的消费者吞吐。内网场景下异步高德对 302 和入库速度几乎无差；公网 IP 才会体现「不再 3 秒卡住消费」。

## 对项目

- 跳转热路径：再次确认可用。
- 「MQ 削峰填谷」：跳转不堵，入库仍会堆；要追上需要批量写或加消费并行，不是再改高德。
- 这轮没有提交 jtl（体积大）。数字在 `statistics.json`。
